package com.example.mytello;

import android.content.Intent;
import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.EditText;
import android.widget.Toast;

public class mostrar_datos extends add_orden {
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.id.list);

        //declarar variables
        //declarar funcion del boton añadir orden

        //llamada a la funcion mostrar datos
    }
    //Crear funcion de muestra de datos
}